import { createRouter, createWebHistory } from 'vue-router';

const router = createRouter({
  history: createWebHistory('/itp-qualification/'),
  routes: [
    // 您的路由配置
  ],
});

export default router;
